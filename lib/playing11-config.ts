/**
 * How many players must be selected as “Playing 11” before save is allowed.
 *
 * **TEMP (testing):** set to `2` to try the flow with two names. **Revert to `11`** when done.
 */
export const REQUIRED_PLAYING11_COUNT = 11;
